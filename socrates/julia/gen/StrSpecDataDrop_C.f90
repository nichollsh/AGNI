! Autogenerated from Fortran file src/radiance_core/def_spectrum.F90
! svn revision 1226

module StrSpecDataDrop_C

use, intrinsic :: iso_c_binding

use UTILITIES_CF
use def_spectrum

implicit none

private

contains

    function StrSpecDataDrop_get_logical_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataDrop_get_logical_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'l_drop_type') then
            if (allocated(spectrum%Drop%l_drop_type)) then
                loc = C_LOC(spectrum%Drop%l_drop_type)
                ndim = size(shape(spectrum%Drop%l_drop_type))
                dims(1:ndim) = shape(spectrum%Drop%l_drop_type)
                lbounds(1:ndim) = lbound(spectrum%Drop%l_drop_type)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataDrop_get_logical_array

    function StrSpecDataDrop_get_integer_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataDrop_get_integer_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'i_drop_parm') then
            if (allocated(spectrum%Drop%i_drop_parm)) then
                loc = C_LOC(spectrum%Drop%i_drop_parm)
                ndim = size(shape(spectrum%Drop%i_drop_parm))
                dims(1:ndim) = shape(spectrum%Drop%i_drop_parm)
                lbounds(1:ndim) = lbound(spectrum%Drop%i_drop_parm)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'n_phf') then
            if (allocated(spectrum%Drop%n_phf)) then
                loc = C_LOC(spectrum%Drop%n_phf)
                ndim = size(shape(spectrum%Drop%n_phf))
                dims(1:ndim) = shape(spectrum%Drop%n_phf)
                lbounds(1:ndim) = lbound(spectrum%Drop%n_phf)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataDrop_get_integer_array

    function StrSpecDataDrop_get_real_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataDrop_get_real_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'parm_list') then
            if (allocated(spectrum%Drop%parm_list)) then
                loc = C_LOC(spectrum%Drop%parm_list)
                ndim = size(shape(spectrum%Drop%parm_list))
                dims(1:ndim) = shape(spectrum%Drop%parm_list)
                lbounds(1:ndim) = lbound(spectrum%Drop%parm_list)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'parm_min_dim') then
            if (allocated(spectrum%Drop%parm_min_dim)) then
                loc = C_LOC(spectrum%Drop%parm_min_dim)
                ndim = size(shape(spectrum%Drop%parm_min_dim))
                dims(1:ndim) = shape(spectrum%Drop%parm_min_dim)
                lbounds(1:ndim) = lbound(spectrum%Drop%parm_min_dim)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'parm_max_dim') then
            if (allocated(spectrum%Drop%parm_max_dim)) then
                loc = C_LOC(spectrum%Drop%parm_max_dim)
                ndim = size(shape(spectrum%Drop%parm_max_dim))
                dims(1:ndim) = shape(spectrum%Drop%parm_max_dim)
                lbounds(1:ndim) = lbound(spectrum%Drop%parm_max_dim)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataDrop_get_real_array

end module StrSpecDataDrop_C
