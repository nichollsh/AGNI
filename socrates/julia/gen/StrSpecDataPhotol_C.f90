! Autogenerated from Fortran file src/radiance_core/def_spectrum.F90
! svn revision 1226

module StrSpecDataPhotol_C

use, intrinsic :: iso_c_binding

use UTILITIES_CF
use def_spectrum

implicit none

private

contains

    function StrSpecDataPhotol_get_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPhotol_get_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), intent(out) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'n_pathway') then
            val = spectrum%Photol%n_pathway
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPhotol_get_integer

    function StrSpecDataPhotol_set_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPhotol_set_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), value, intent(in) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'n_pathway') then
            spectrum%Photol%n_pathway = val
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPhotol_set_integer

    function StrSpecDataPhotol_get_logical_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPhotol_get_logical_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'l_thermalise') then
            if (allocated(spectrum%Photol%l_thermalise)) then
                loc = C_LOC(spectrum%Photol%l_thermalise)
                ndim = size(shape(spectrum%Photol%l_thermalise))
                dims(1:ndim) = shape(spectrum%Photol%l_thermalise)
                lbounds(1:ndim) = lbound(spectrum%Photol%l_thermalise)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPhotol_get_logical_array

    function StrSpecDataPhotol_get_integer_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPhotol_get_integer_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'pathway_absorber') then
            if (allocated(spectrum%Photol%pathway_absorber)) then
                loc = C_LOC(spectrum%Photol%pathway_absorber)
                ndim = size(shape(spectrum%Photol%pathway_absorber))
                dims(1:ndim) = shape(spectrum%Photol%pathway_absorber)
                lbounds(1:ndim) = lbound(spectrum%Photol%pathway_absorber)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'pathway_products') then
            if (allocated(spectrum%Photol%pathway_products)) then
                loc = C_LOC(spectrum%Photol%pathway_products)
                ndim = size(shape(spectrum%Photol%pathway_products))
                dims(1:ndim) = shape(spectrum%Photol%pathway_products)
                lbounds(1:ndim) = lbound(spectrum%Photol%pathway_products)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'n_t_lookup_photol') then
            if (allocated(spectrum%Photol%n_t_lookup_photol)) then
                loc = C_LOC(spectrum%Photol%n_t_lookup_photol)
                ndim = size(shape(spectrum%Photol%n_t_lookup_photol))
                dims(1:ndim) = shape(spectrum%Photol%n_t_lookup_photol)
                lbounds(1:ndim) = lbound(spectrum%Photol%n_t_lookup_photol)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'n_wl_lookup_photol') then
            if (allocated(spectrum%Photol%n_wl_lookup_photol)) then
                loc = C_LOC(spectrum%Photol%n_wl_lookup_photol)
                ndim = size(shape(spectrum%Photol%n_wl_lookup_photol))
                dims(1:ndim) = shape(spectrum%Photol%n_wl_lookup_photol)
                lbounds(1:ndim) = lbound(spectrum%Photol%n_wl_lookup_photol)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'qy_sub') then
            if (allocated(spectrum%Photol%qy_sub)) then
                loc = C_LOC(spectrum%Photol%qy_sub)
                ndim = size(shape(spectrum%Photol%qy_sub))
                dims(1:ndim) = shape(spectrum%Photol%qy_sub)
                lbounds(1:ndim) = lbound(spectrum%Photol%qy_sub)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPhotol_get_integer_array

    function StrSpecDataPhotol_get_real_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPhotol_get_real_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 't_lookup_photol') then
            if (allocated(spectrum%Photol%t_lookup_photol)) then
                loc = C_LOC(spectrum%Photol%t_lookup_photol)
                ndim = size(shape(spectrum%Photol%t_lookup_photol))
                dims(1:ndim) = shape(spectrum%Photol%t_lookup_photol)
                lbounds(1:ndim) = lbound(spectrum%Photol%t_lookup_photol)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'wl_lookup_photol') then
            if (allocated(spectrum%Photol%wl_lookup_photol)) then
                loc = C_LOC(spectrum%Photol%wl_lookup_photol)
                ndim = size(shape(spectrum%Photol%wl_lookup_photol))
                dims(1:ndim) = shape(spectrum%Photol%wl_lookup_photol)
                lbounds(1:ndim) = lbound(spectrum%Photol%wl_lookup_photol)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'quantum_yield') then
            if (allocated(spectrum%Photol%quantum_yield)) then
                loc = C_LOC(spectrum%Photol%quantum_yield)
                ndim = size(shape(spectrum%Photol%quantum_yield))
                dims(1:ndim) = shape(spectrum%Photol%quantum_yield)
                lbounds(1:ndim) = lbound(spectrum%Photol%quantum_yield)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'threshold_wavelength') then
            if (allocated(spectrum%Photol%threshold_wavelength)) then
                loc = C_LOC(spectrum%Photol%threshold_wavelength)
                ndim = size(shape(spectrum%Photol%threshold_wavelength))
                dims(1:ndim) = shape(spectrum%Photol%threshold_wavelength)
                lbounds(1:ndim) = lbound(spectrum%Photol%threshold_wavelength)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPhotol_get_real_array

end module StrSpecDataPhotol_C
