! Autogenerated from Fortran file src/radiance_core/def_spectrum.F90
! svn revision 1226

module StrSpecDataSolar_C

use, intrinsic :: iso_c_binding

use UTILITIES_CF
use def_spectrum

implicit none

private

contains

    function StrSpecDataSolar_get_real_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataSolar_get_real_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'solar_flux_band') then
            if (allocated(spectrum%Solar%solar_flux_band)) then
                loc = C_LOC(spectrum%Solar%solar_flux_band)
                ndim = size(shape(spectrum%Solar%solar_flux_band))
                dims(1:ndim) = shape(spectrum%Solar%solar_flux_band)
                lbounds(1:ndim) = lbound(spectrum%Solar%solar_flux_band)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'solar_flux_band_ses') then
            if (allocated(spectrum%Solar%solar_flux_band_ses)) then
                loc = C_LOC(spectrum%Solar%solar_flux_band_ses)
                ndim = size(shape(spectrum%Solar%solar_flux_band_ses))
                dims(1:ndim) = shape(spectrum%Solar%solar_flux_band_ses)
                lbounds(1:ndim) = lbound(spectrum%Solar%solar_flux_band_ses)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'weight_blue') then
            if (allocated(spectrum%Solar%weight_blue)) then
                loc = C_LOC(spectrum%Solar%weight_blue)
                ndim = size(shape(spectrum%Solar%weight_blue))
                dims(1:ndim) = shape(spectrum%Solar%weight_blue)
                lbounds(1:ndim) = lbound(spectrum%Solar%weight_blue)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataSolar_get_real_array

end module StrSpecDataSolar_C
