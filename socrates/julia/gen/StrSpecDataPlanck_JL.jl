# Autogenerated from src/radiance_core/def_spectrum.F90
# svn revision 1226


    mutable struct StrSpecDataPlanck{P}
        parent_handle::P
    end


    # this is used to show values in the REPL and when using IJulia
    function Base.show(io::IO, m::MIME"text/plain", handle::StrSpecDataPlanck)
        println(io, handle)
        dump_properties(io, handle)                
    end


    function Base.propertynames(handle::StrSpecDataPlanck, private::Bool=false)
        names = [
            :n_deg_fit,
            :thermal_coeff,
            :theta_planck_tbl,
            :t_ref_planck,
            :l_planck_tbl,
        ]

        return names
    end


    function Base.getproperty(handle::StrSpecDataPlanck, field::Symbol)


        cptr = getfield(getfield(handle, :parent_handle), :cptr)
        cptr != Ptr{Cvoid}() || error("invalid handle (null cptr)")            

        if field in (
            :l_planck_tbl,
        )
            
            val = Ref{Cuchar}()
            field_ok = ccall(
                (:PS_StrSpecDataPlanck_get_logical, libSOCRATES_C),
                Cuchar,
                (Ptr{Cvoid}, Cstring, Ref{Cuchar}),
                cptr, String(field), val
            )
            Bool(field_ok) || error("StrSpecDataPlanck logical field $field not present - coding error")
            return Bool(val[])
        elseif field in (
            :n_deg_fit,
        )
            val = Ref{Cint}()
            field_ok = ccall(
                (:PS_StrSpecDataPlanck_get_integer, libSOCRATES_C),
                Cuchar,
                (Ptr{Cvoid}, Cstring, Ref{Cint}),
                cptr, String(field), val
            )
            Bool(field_ok) || error("StrSpecDataPlanck integer field $field not present - coding error")
            return val[]
        elseif field in (
            :t_ref_planck,
        )
            val = Ref{Cdouble}()
            field_ok = ccall(
                (:PS_StrSpecDataPlanck_get_real, libSOCRATES_C),
                Cuchar,
                (Ptr{Cvoid}, Cstring, Ref{Cdouble}),
                cptr, String(field), val
            )
            Bool(field_ok) || error("StrSpecDataPlanck real field $field not present - coding error")
            return val[]
        elseif field in (
            :thermal_coeff,
            :theta_planck_tbl,
        )
            loc = Ref{Ptr{Cdouble}}()
            ndim = Ref{Cint}(10)
            dims = zeros(Cint, ndim[])
            lbounds = zeros(Cint, ndim[])
            field_ok = ccall(
                (:PS_StrSpecDataPlanck_get_real_array, libSOCRATES_C),
                Cuchar, 
                (Ptr{Cvoid}, Cstring, Ref{Ptr{Cdouble}}, Ref{Cint}, Ref{Cint}, Ref{Cint}),
                cptr, String(field), loc, dims, ndim, lbounds
            )
            Bool(field_ok) || error("StrSpecDataPlanck Cdouble array field $field not present - coding error")
            if loc[] == C_NULL
                return nothing
            else
                a = unsafe_wrap(Array, loc[], Tuple(dims[1:ndim[]]), own=false)
                oa = OffsetArray(a, (lbounds[1:ndim[]] .- 1)...)
                return oa
            end
        else
            return getfield(handle, field)
        end    
           
    end


    function Base.setproperty!(handle::StrSpecDataPlanck, field::Symbol, val)
              

        cptr = getfield(getfield(handle, :parent_handle), :cptr)
        cptr != Ptr{Cvoid}() || error("invalid handle (null cptr)")            

        if field in (
            :l_planck_tbl,
        )
                                
            field_ok = ccall(
                (:PS_StrSpecDataPlanck_set_logical, libSOCRATES_C),
                Cuchar, 
                (Ptr{Cvoid}, Cstring, Cuchar),
                cptr, String(field), val
            )
            Bool(field_ok) || error("StrSpecDataPlanck logical field $field not present - coding error")
            return val
        elseif field in (
            :n_deg_fit,
        )
                    
            field_ok = ccall(
                (:PS_StrSpecDataPlanck_set_integer, libSOCRATES_C),
                Cuchar,
                (Ptr{Cvoid}, Cstring, Cint),
                cptr, String(field), val
            )
            Bool(field_ok) || error("StrSpecDataPlanck integer field $field not present - coding error")
            return val
        elseif field in (
            :t_ref_planck,
        )
                    
            field_ok = ccall(
                (:PS_StrSpecDataPlanck_set_real, libSOCRATES_C),
                Cuchar,
                (Ptr{Cvoid}, Cstring, Cdouble),
                cptr, String(field), val
            )
            Bool(field_ok) || error("StrSpecDataPlanck real field $field not present - coding error")
            return val
        else
            error("type StrSpecDataPlanck has no writeable field $field")    
        end    
           
    end

