! Autogenerated from Fortran file src/radiance_core/def_spectrum.F90
! svn revision 1226

module StrSpecDataDim_C

use, intrinsic :: iso_c_binding

use UTILITIES_CF
use def_spectrum

implicit none

private

contains

    function StrSpecDataDim_get_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataDim_get_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), intent(out) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'nd_alloc_int') then
            val = spectrum%Dim%nd_alloc_int
        else if (field == 'nd_alloc_real') then
            val = spectrum%Dim%nd_alloc_real
        else if (field == 'nd_alloc_log') then
            val = spectrum%Dim%nd_alloc_log
        else if (field == 'nd_type') then
            val = spectrum%Dim%nd_type
        else if (field == 'nd_band') then
            val = spectrum%Dim%nd_band
        else if (field == 'nd_exclude') then
            val = spectrum%Dim%nd_exclude
        else if (field == 'nd_k_term') then
            val = spectrum%Dim%nd_k_term
        else if (field == 'nd_species') then
            val = spectrum%Dim%nd_species
        else if (field == 'nd_scale_variable') then
            val = spectrum%Dim%nd_scale_variable
        else if (field == 'nd_continuum') then
            val = spectrum%Dim%nd_continuum
        else if (field == 'nd_drop_type') then
            val = spectrum%Dim%nd_drop_type
        else if (field == 'nd_ice_type') then
            val = spectrum%Dim%nd_ice_type
        else if (field == 'nd_aerosol_species') then
            val = spectrum%Dim%nd_aerosol_species
        else if (field == 'nd_aerosol_mr') then
            val = spectrum%Dim%nd_aerosol_mr
        else if (field == 'nd_thermal_coeff') then
            val = spectrum%Dim%nd_thermal_coeff
        else if (field == 'nd_cloud_parameter') then
            val = spectrum%Dim%nd_cloud_parameter
        else if (field == 'nd_humidity') then
            val = spectrum%Dim%nd_humidity
        else if (field == 'nd_aod_wavel') then
            val = spectrum%Dim%nd_aod_wavel
        else if (field == 'nd_phase_term') then
            val = spectrum%Dim%nd_phase_term
        else if (field == 'nd_tmp') then
            val = spectrum%Dim%nd_tmp
        else if (field == 'nd_pre') then
            val = spectrum%Dim%nd_pre
        else if (field == 'nd_mix') then
            val = spectrum%Dim%nd_mix
        else if (field == 'nd_band_mix_gas') then
            val = spectrum%Dim%nd_band_mix_gas
        else if (field == 'nd_sub_band_k') then
            val = spectrum%Dim%nd_sub_band_k
        else if (field == 'nd_k_sub_band') then
            val = spectrum%Dim%nd_k_sub_band
        else if (field == 'nd_sub_band_gas') then
            val = spectrum%Dim%nd_sub_band_gas
        else if (field == 'nd_sub_band') then
            val = spectrum%Dim%nd_sub_band
        else if (field == 'nd_times') then
            val = spectrum%Dim%nd_times
        else if (field == 'nd_cont') then
            val = spectrum%Dim%nd_cont
        else if (field == 'nd_t_lookup_cont') then
            val = spectrum%Dim%nd_t_lookup_cont
        else if (field == 'nd_k_term_cont') then
            val = spectrum%Dim%nd_k_term_cont
        else if (field == 'nd_species_sb') then
            val = spectrum%Dim%nd_species_sb
        else if (field == 'nd_gas_frac') then
            val = spectrum%Dim%nd_gas_frac
        else if (field == 'nd_pathway') then
            val = spectrum%Dim%nd_pathway
        else if (field == 'nd_t_lookup_photol') then
            val = spectrum%Dim%nd_t_lookup_photol
        else if (field == 'nd_wl_lookup_photol') then
            val = spectrum%Dim%nd_wl_lookup_photol
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataDim_get_integer

    function StrSpecDataDim_set_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataDim_set_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), value, intent(in) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'nd_alloc_int') then
            spectrum%Dim%nd_alloc_int = val
        else if (field == 'nd_alloc_real') then
            spectrum%Dim%nd_alloc_real = val
        else if (field == 'nd_alloc_log') then
            spectrum%Dim%nd_alloc_log = val
        else if (field == 'nd_type') then
            spectrum%Dim%nd_type = val
        else if (field == 'nd_band') then
            spectrum%Dim%nd_band = val
        else if (field == 'nd_exclude') then
            spectrum%Dim%nd_exclude = val
        else if (field == 'nd_k_term') then
            spectrum%Dim%nd_k_term = val
        else if (field == 'nd_species') then
            spectrum%Dim%nd_species = val
        else if (field == 'nd_scale_variable') then
            spectrum%Dim%nd_scale_variable = val
        else if (field == 'nd_continuum') then
            spectrum%Dim%nd_continuum = val
        else if (field == 'nd_drop_type') then
            spectrum%Dim%nd_drop_type = val
        else if (field == 'nd_ice_type') then
            spectrum%Dim%nd_ice_type = val
        else if (field == 'nd_aerosol_species') then
            spectrum%Dim%nd_aerosol_species = val
        else if (field == 'nd_aerosol_mr') then
            spectrum%Dim%nd_aerosol_mr = val
        else if (field == 'nd_thermal_coeff') then
            spectrum%Dim%nd_thermal_coeff = val
        else if (field == 'nd_cloud_parameter') then
            spectrum%Dim%nd_cloud_parameter = val
        else if (field == 'nd_humidity') then
            spectrum%Dim%nd_humidity = val
        else if (field == 'nd_aod_wavel') then
            spectrum%Dim%nd_aod_wavel = val
        else if (field == 'nd_phase_term') then
            spectrum%Dim%nd_phase_term = val
        else if (field == 'nd_tmp') then
            spectrum%Dim%nd_tmp = val
        else if (field == 'nd_pre') then
            spectrum%Dim%nd_pre = val
        else if (field == 'nd_mix') then
            spectrum%Dim%nd_mix = val
        else if (field == 'nd_band_mix_gas') then
            spectrum%Dim%nd_band_mix_gas = val
        else if (field == 'nd_sub_band_k') then
            spectrum%Dim%nd_sub_band_k = val
        else if (field == 'nd_k_sub_band') then
            spectrum%Dim%nd_k_sub_band = val
        else if (field == 'nd_sub_band_gas') then
            spectrum%Dim%nd_sub_band_gas = val
        else if (field == 'nd_sub_band') then
            spectrum%Dim%nd_sub_band = val
        else if (field == 'nd_times') then
            spectrum%Dim%nd_times = val
        else if (field == 'nd_cont') then
            spectrum%Dim%nd_cont = val
        else if (field == 'nd_t_lookup_cont') then
            spectrum%Dim%nd_t_lookup_cont = val
        else if (field == 'nd_k_term_cont') then
            spectrum%Dim%nd_k_term_cont = val
        else if (field == 'nd_species_sb') then
            spectrum%Dim%nd_species_sb = val
        else if (field == 'nd_gas_frac') then
            spectrum%Dim%nd_gas_frac = val
        else if (field == 'nd_pathway') then
            spectrum%Dim%nd_pathway = val
        else if (field == 'nd_t_lookup_photol') then
            spectrum%Dim%nd_t_lookup_photol = val
        else if (field == 'nd_wl_lookup_photol') then
            spectrum%Dim%nd_wl_lookup_photol = val
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataDim_set_integer

end module StrSpecDataDim_C
