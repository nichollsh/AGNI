! Autogenerated from Fortran file src/radiance_core/def_spectrum.F90
! svn revision 1226

module StrSpecDataVar_C

use, intrinsic :: iso_c_binding

use UTILITIES_CF
use def_spectrum

implicit none

private

contains

    function StrSpecDataVar_get_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataVar_get_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), intent(out) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'n_sub_band') then
            val = spectrum%Var%n_sub_band
        else if (field == 'n_times') then
            val = spectrum%Var%n_times
        else if (field == 'n_repeat_times') then
            val = spectrum%Var%n_repeat_times
        else if (field == 'n_rayleigh_coeff') then
            val = spectrum%Var%n_rayleigh_coeff
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataVar_get_integer

    function StrSpecDataVar_set_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataVar_set_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), value, intent(in) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'n_sub_band') then
            spectrum%Var%n_sub_band = val
        else if (field == 'n_times') then
            spectrum%Var%n_times = val
        else if (field == 'n_repeat_times') then
            spectrum%Var%n_repeat_times = val
        else if (field == 'n_rayleigh_coeff') then
            spectrum%Var%n_rayleigh_coeff = val
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataVar_set_integer

    function StrSpecDataVar_get_integer_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataVar_get_integer_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'index_sub_band') then
            if (allocated(spectrum%Var%index_sub_band)) then
                loc = C_LOC(spectrum%Var%index_sub_band)
                ndim = size(shape(spectrum%Var%index_sub_band))
                dims(1:ndim) = shape(spectrum%Var%index_sub_band)
                lbounds(1:ndim) = lbound(spectrum%Var%index_sub_band)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'time') then
            if (allocated(spectrum%Var%time)) then
                loc = C_LOC(spectrum%Var%time)
                ndim = size(shape(spectrum%Var%time))
                dims(1:ndim) = shape(spectrum%Var%time)
                lbounds(1:ndim) = lbound(spectrum%Var%time)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataVar_get_integer_array

    function StrSpecDataVar_get_real_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataVar_get_real_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'wavelength_sub_band') then
            if (allocated(spectrum%Var%wavelength_sub_band)) then
                loc = C_LOC(spectrum%Var%wavelength_sub_band)
                ndim = size(shape(spectrum%Var%wavelength_sub_band))
                dims(1:ndim) = shape(spectrum%Var%wavelength_sub_band)
                lbounds(1:ndim) = lbound(spectrum%Var%wavelength_sub_band)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'total_solar_flux') then
            if (allocated(spectrum%Var%total_solar_flux)) then
                loc = C_LOC(spectrum%Var%total_solar_flux)
                ndim = size(shape(spectrum%Var%total_solar_flux))
                dims(1:ndim) = shape(spectrum%Var%total_solar_flux)
                lbounds(1:ndim) = lbound(spectrum%Var%total_solar_flux)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'solar_flux_sub_band') then
            if (allocated(spectrum%Var%solar_flux_sub_band)) then
                loc = C_LOC(spectrum%Var%solar_flux_sub_band)
                ndim = size(shape(spectrum%Var%solar_flux_sub_band))
                dims(1:ndim) = shape(spectrum%Var%solar_flux_sub_band)
                lbounds(1:ndim) = lbound(spectrum%Var%solar_flux_sub_band)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'rayleigh_coeff') then
            if (allocated(spectrum%Var%rayleigh_coeff)) then
                loc = C_LOC(spectrum%Var%rayleigh_coeff)
                ndim = size(shape(spectrum%Var%rayleigh_coeff))
                dims(1:ndim) = shape(spectrum%Var%rayleigh_coeff)
                lbounds(1:ndim) = lbound(spectrum%Var%rayleigh_coeff)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataVar_get_real_array

end module StrSpecDataVar_C
