! Autogenerated from Fortran file src/radiance_core/def_spectrum.F90
! svn revision 1226

module StrSpecDataContGen_C

use, intrinsic :: iso_c_binding

use UTILITIES_CF
use def_spectrum

implicit none

private

contains

    function StrSpecDataContGen_get_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataContGen_get_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), intent(out) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'n_cont') then
            val = spectrum%ContGen%n_cont
        else if (field == 'n_t_lookup_cont') then
            val = spectrum%ContGen%n_t_lookup_cont
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataContGen_get_integer

    function StrSpecDataContGen_set_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataContGen_set_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), value, intent(in) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'n_cont') then
            spectrum%ContGen%n_cont = val
        else if (field == 'n_t_lookup_cont') then
            spectrum%ContGen%n_t_lookup_cont = val
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataContGen_set_integer

    function StrSpecDataContGen_get_logical_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataContGen_get_logical_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'l_cont_major') then
            if (allocated(spectrum%ContGen%l_cont_major)) then
                loc = C_LOC(spectrum%ContGen%l_cont_major)
                ndim = size(shape(spectrum%ContGen%l_cont_major))
                dims(1:ndim) = shape(spectrum%ContGen%l_cont_major)
                lbounds(1:ndim) = lbound(spectrum%ContGen%l_cont_major)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataContGen_get_logical_array

    function StrSpecDataContGen_get_integer_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataContGen_get_integer_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'n_band_cont') then
            if (allocated(spectrum%ContGen%n_band_cont)) then
                loc = C_LOC(spectrum%ContGen%n_band_cont)
                ndim = size(shape(spectrum%ContGen%n_band_cont))
                dims(1:ndim) = shape(spectrum%ContGen%n_band_cont)
                lbounds(1:ndim) = lbound(spectrum%ContGen%n_band_cont)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'index_cont') then
            if (allocated(spectrum%ContGen%index_cont)) then
                loc = C_LOC(spectrum%ContGen%index_cont)
                ndim = size(shape(spectrum%ContGen%index_cont))
                dims(1:ndim) = shape(spectrum%ContGen%index_cont)
                lbounds(1:ndim) = lbound(spectrum%ContGen%index_cont)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'index_cont_gas_1') then
            if (allocated(spectrum%ContGen%index_cont_gas_1)) then
                loc = C_LOC(spectrum%ContGen%index_cont_gas_1)
                ndim = size(shape(spectrum%ContGen%index_cont_gas_1))
                dims(1:ndim) = shape(spectrum%ContGen%index_cont_gas_1)
                lbounds(1:ndim) = lbound(spectrum%ContGen%index_cont_gas_1)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'index_cont_gas_2') then
            if (allocated(spectrum%ContGen%index_cont_gas_2)) then
                loc = C_LOC(spectrum%ContGen%index_cont_gas_2)
                ndim = size(shape(spectrum%ContGen%index_cont_gas_2))
                dims(1:ndim) = shape(spectrum%ContGen%index_cont_gas_2)
                lbounds(1:ndim) = lbound(spectrum%ContGen%index_cont_gas_2)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'i_band_k_cont') then
            if (allocated(spectrum%ContGen%i_band_k_cont)) then
                loc = C_LOC(spectrum%ContGen%i_band_k_cont)
                ndim = size(shape(spectrum%ContGen%i_band_k_cont))
                dims(1:ndim) = shape(spectrum%ContGen%i_band_k_cont)
                lbounds(1:ndim) = lbound(spectrum%ContGen%i_band_k_cont)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'i_cont_overlap_band') then
            if (allocated(spectrum%ContGen%i_cont_overlap_band)) then
                loc = C_LOC(spectrum%ContGen%i_cont_overlap_band)
                ndim = size(shape(spectrum%ContGen%i_cont_overlap_band))
                dims(1:ndim) = shape(spectrum%ContGen%i_cont_overlap_band)
                lbounds(1:ndim) = lbound(spectrum%ContGen%i_cont_overlap_band)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'i_scat_cont') then
            if (allocated(spectrum%ContGen%i_scat_cont)) then
                loc = C_LOC(spectrum%ContGen%i_scat_cont)
                ndim = size(shape(spectrum%ContGen%i_scat_cont))
                dims(1:ndim) = shape(spectrum%ContGen%i_scat_cont)
                lbounds(1:ndim) = lbound(spectrum%ContGen%i_scat_cont)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataContGen_get_integer_array

    function StrSpecDataContGen_get_real_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataContGen_get_real_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'k_cont') then
            if (allocated(spectrum%ContGen%k_cont)) then
                loc = C_LOC(spectrum%ContGen%k_cont)
                ndim = size(shape(spectrum%ContGen%k_cont))
                dims(1:ndim) = shape(spectrum%ContGen%k_cont)
                lbounds(1:ndim) = lbound(spectrum%ContGen%k_cont)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'w_cont') then
            if (allocated(spectrum%ContGen%w_cont)) then
                loc = C_LOC(spectrum%ContGen%w_cont)
                ndim = size(shape(spectrum%ContGen%w_cont))
                dims(1:ndim) = shape(spectrum%ContGen%w_cont)
                lbounds(1:ndim) = lbound(spectrum%ContGen%w_cont)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 't_lookup_cont') then
            if (allocated(spectrum%ContGen%t_lookup_cont)) then
                loc = C_LOC(spectrum%ContGen%t_lookup_cont)
                ndim = size(shape(spectrum%ContGen%t_lookup_cont))
                dims(1:ndim) = shape(spectrum%ContGen%t_lookup_cont)
                lbounds(1:ndim) = lbound(spectrum%ContGen%t_lookup_cont)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'k_lookup_cont') then
            if (allocated(spectrum%ContGen%k_lookup_cont)) then
                loc = C_LOC(spectrum%ContGen%k_lookup_cont)
                ndim = size(shape(spectrum%ContGen%k_lookup_cont))
                dims(1:ndim) = shape(spectrum%ContGen%k_lookup_cont)
                lbounds(1:ndim) = lbound(spectrum%ContGen%k_lookup_cont)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataContGen_get_real_array

end module StrSpecDataContGen_C
