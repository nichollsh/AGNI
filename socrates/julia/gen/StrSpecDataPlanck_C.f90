! Autogenerated from Fortran file src/radiance_core/def_spectrum.F90
! svn revision 1226

module StrSpecDataPlanck_C

use, intrinsic :: iso_c_binding

use UTILITIES_CF
use def_spectrum

implicit none

private

contains

    function StrSpecDataPlanck_get_logical(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPlanck_get_logical')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        logical(c_bool), intent(out) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'l_planck_tbl') then
            val = spectrum%Planck%l_planck_tbl
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPlanck_get_logical

    function StrSpecDataPlanck_set_logical(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPlanck_set_logical')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        logical(c_bool), value, intent(in) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'l_planck_tbl') then
            spectrum%Planck%l_planck_tbl = val
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPlanck_set_logical

    function StrSpecDataPlanck_get_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPlanck_get_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), intent(out) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'n_deg_fit') then
            val = spectrum%Planck%n_deg_fit
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPlanck_get_integer

    function StrSpecDataPlanck_set_integer(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPlanck_set_integer')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        integer(c_int), value, intent(in) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 'n_deg_fit') then
            spectrum%Planck%n_deg_fit = val
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPlanck_set_integer

    function StrSpecDataPlanck_get_real(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPlanck_get_real')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        real(c_double), intent(out) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 't_ref_planck') then
            val = spectrum%Planck%t_ref_planck
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPlanck_get_real

    function StrSpecDataPlanck_set_real(spectrum_Cptr, field_Cstr, val) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPlanck_set_real')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        real(c_double), value, intent(in) :: val

        ! local variables
        TYPE(StrSpecData), pointer                  :: spectrum
        character(len=:), allocatable           :: field

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)

        field_OK = .TRUE.
    
        if (field == 't_ref_planck') then
            spectrum%Planck%t_ref_planck = val
        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPlanck_set_real

    function StrSpecDataPlanck_get_real_array(spectrum_Cptr, field_Cstr, loc, dims_C, ndim, lbounds_C) result(field_OK) &
        bind(C, NAME='PS_StrSpecDataPlanck_get_real_array')

        implicit none
        type(c_ptr), value, intent(in)          :: spectrum_Cptr
        type(c_ptr), value, intent(in)          :: field_Cstr
        logical(c_bool)                         :: field_OK
        type(c_ptr), intent(out)                :: loc
        type(c_ptr), value, intent(in)          :: dims_C
        integer(c_int), intent(inout)           :: ndim
        type(c_ptr), value, intent(in)          :: lbounds_C

        ! local variables
        TYPE(StrSpecData), pointer             :: spectrum
        character(len=:), allocatable           :: field
        integer(c_int), pointer, dimension(:)   :: dims, lbounds    

        ! convert types
        call C_F_POINTER(spectrum_Cptr, spectrum)
        field = c_to_f_string(field_Cstr)
        call C_F_POINTER(dims_C, dims, [ndim])
        call C_F_POINTER(lbounds_C, lbounds, [ndim])

        field_OK = .TRUE.
    
        if (field == 'thermal_coeff') then
            if (allocated(spectrum%Planck%thermal_coeff)) then
                loc = C_LOC(spectrum%Planck%thermal_coeff)
                ndim = size(shape(spectrum%Planck%thermal_coeff))
                dims(1:ndim) = shape(spectrum%Planck%thermal_coeff)
                lbounds(1:ndim) = lbound(spectrum%Planck%thermal_coeff)
            else
                loc = C_NULL_PTR
            end if

        else if (field == 'theta_planck_tbl') then
            if (allocated(spectrum%Planck%theta_planck_tbl)) then
                loc = C_LOC(spectrum%Planck%theta_planck_tbl)
                ndim = size(shape(spectrum%Planck%theta_planck_tbl))
                dims(1:ndim) = shape(spectrum%Planck%theta_planck_tbl)
                lbounds(1:ndim) = lbound(spectrum%Planck%theta_planck_tbl)
            else
                loc = C_NULL_PTR
            end if

        else
            field_OK = .FALSE.
        end if

    end function StrSpecDataPlanck_get_real_array

end module StrSpecDataPlanck_C
