# Autogenerated from src/radiance_core/def_bound.F90
# svn revision 1226


    mutable struct StrBound
        cptr::Ptr{Cvoid}

        function StrBound()
            handle = new(ccall((:PS_create_StrBound, libSOCRATES_C), Ptr{Cvoid}, ()))
            finalizer(delete_StrBound, handle)
            return handle
        end
    end

    function delete_StrBound(handle::StrBound)
        if getfield(handle, :cptr) == Ptr{Cvoid}()
            ccall(:jl_safe_printf, Cvoid, (Cstring, ), "error: delete_StrBound attempt to delete null pointer")
        else
            ccall((:PS_delete_StrBound, libSOCRATES_C), Cvoid, (Ptr{Cvoid},), getfield(handle, :cptr))
            setfield!(handle, :cptr, Ptr{Cvoid}())
        end
        return nothing
    end


    # this is used to show values in the REPL and when using IJulia
    function Base.show(io::IO, m::MIME"text/plain", handle::StrBound)
        println(io, handle)
        dump_properties(io, handle)                
    end


    function Base.propertynames(handle::StrBound, private::Bool=false)
        names = [
            :zen_0,
            :azim_0,
            :solar_irrad,
            :lit,
            :cos_zen,
            :n_brdf_basis_fnc,
            :rho_alb,
            :f_brdf,
            :t_ground,
            :flux_ground,
            :orog_corr,
            :n_point_tile,
            :n_tile,
            :list_tile,
            :rho_alb_tile,
            :frac_tile,
            :t_tile,
            :flux_tile,
        ]

        return names
    end


    function Base.getproperty(handle::StrBound, field::Symbol)


        cptr = getfield(handle, :cptr)
        cptr != Ptr{Cvoid}() || error("invalid handle (null cptr)")            


        if field == :cptr
            return cptr

        elseif field in (
            :n_brdf_basis_fnc,
            :n_point_tile,
            :n_tile,
        )
            val = Ref{Cint}()
            field_ok = ccall(
                (:PS_StrBound_get_integer, libSOCRATES_C),
                Cuchar,
                (Ptr{Cvoid}, Cstring, Ref{Cint}),
                cptr, String(field), val
            )
            Bool(field_ok) || error("StrBound integer field $field not present - coding error")
            return val[]
        elseif field in (
            :list_tile,
        )
            loc = Ref{Ptr{Cint}}()
            ndim = Ref{Cint}(10)
            dims = zeros(Cint, ndim[])
            lbounds = zeros(Cint, ndim[])
            field_ok = ccall(
                (:PS_StrBound_get_integer_array, libSOCRATES_C),
                Cuchar, 
                (Ptr{Cvoid}, Cstring, Ref{Ptr{Cint}}, Ref{Cint}, Ref{Cint}, Ref{Cint}),
                cptr, String(field), loc, dims, ndim, lbounds
            )
            Bool(field_ok) || error("StrBound Cint array field $field not present - coding error")
            if loc[] == C_NULL
                return nothing
            else
                a = unsafe_wrap(Array, loc[], Tuple(dims[1:ndim[]]), own=false)
                oa = OffsetArray(a, (lbounds[1:ndim[]] .- 1)...)
                return oa
            end
        elseif field in (
            :zen_0,
            :azim_0,
            :solar_irrad,
            :lit,
            :cos_zen,
            :rho_alb,
            :f_brdf,
            :t_ground,
            :flux_ground,
            :orog_corr,
            :rho_alb_tile,
            :frac_tile,
            :t_tile,
            :flux_tile,
        )
            loc = Ref{Ptr{Cdouble}}()
            ndim = Ref{Cint}(10)
            dims = zeros(Cint, ndim[])
            lbounds = zeros(Cint, ndim[])
            field_ok = ccall(
                (:PS_StrBound_get_real_array, libSOCRATES_C),
                Cuchar, 
                (Ptr{Cvoid}, Cstring, Ref{Ptr{Cdouble}}, Ref{Cint}, Ref{Cint}, Ref{Cint}),
                cptr, String(field), loc, dims, ndim, lbounds
            )
            Bool(field_ok) || error("StrBound Cdouble array field $field not present - coding error")
            if loc[] == C_NULL
                return nothing
            else
                a = unsafe_wrap(Array, loc[], Tuple(dims[1:ndim[]]), own=false)
                oa = OffsetArray(a, (lbounds[1:ndim[]] .- 1)...)
                return oa
            end
        else
            return getfield(handle, field)
        end    
           
    end


    function Base.setproperty!(handle::StrBound, field::Symbol, val)
              

        cptr = getfield(handle, :cptr)
        cptr != Ptr{Cvoid}() || error("invalid handle (null cptr)")            

        if field in (
            :n_brdf_basis_fnc,
            :n_point_tile,
            :n_tile,
        )
                    
            field_ok = ccall(
                (:PS_StrBound_set_integer, libSOCRATES_C),
                Cuchar,
                (Ptr{Cvoid}, Cstring, Cint),
                cptr, String(field), val
            )
            Bool(field_ok) || error("StrBound integer field $field not present - coding error")
            return val
        else
            error("type StrBound has no writeable field $field")    
        end    
           
    end

